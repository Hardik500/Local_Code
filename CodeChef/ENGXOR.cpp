#include <bits/stdc++.h>

using namespace std;

int main(){
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    long long int count, N, Q, P;

    cin >> count;

    for (size_t i = 0; i < count; i++)
    {
        cin >> N >> Q;
        int A[N];

        for (size_t j = 0; j < Q; j++)
        {
            cin >> P;
        }
        
    }
    

    return 0;
    
}